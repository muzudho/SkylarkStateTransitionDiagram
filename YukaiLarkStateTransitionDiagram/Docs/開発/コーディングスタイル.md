# コーディングスタイル

## １ステートメントのアーリーリターンはワンライナー

return 文が１ステートメントのとき、つまり、  

```csharp
if (isBanana) {
	return;
}
```

みたいなときがある。  
このとき、コードブロックにするよりは、  

```csharp
if (isBanana) return;
```

のようにワンライナーにする形を好むぜ（＾～＾）  
`break`, `continue` も同様だぜ（＾～＾）　　
